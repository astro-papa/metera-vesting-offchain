{
  "name": "test",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "type": "module",
  "scripts": {
    "install-packages": "cd .. && npm run repack && cd test && rm -rf node_modules && rm -f package-lock.json && npm i",
    "test": "npm run install-packages && vitest run",
    "test:vitest": "vitest run"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "typescript": "^5.0.4",
    "vitest": "^0.30.1"
  },
  "dependencies": {
    "cherrylend-offchain": "file:../cherrylend-offchain-0.0.1.tgz"
  }
}
