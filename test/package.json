{
  "name": "test",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "type": "module",
  "scripts": {
    "install-packages": "cd .. && npm run repack && cd test && rm -rf node_modules && rm -f package-lock.json && npm i",
    "test": "npm run install-packages && npm run vitest",
    "vitest": "export NODE_ENV='emulator' && vitest run"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "typescript": "^5.0.4",
    "vitest": "^0.30.1"
  },
  "dependencies": {
    "linear-vesting-offchain": "file:../linear-vesting-offchain-0.0.6.tgz"
  }
}
